@layout CarLayout

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Cars</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<!-- Mobile-friendly car menu that preserves original structure -->
<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        <div class="nav-category"> Car</div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="cars/add">
                <span class="bi bi-plus-circle" aria-hidden="true"></span>Add Car
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="cars">
                <span class="bi bi-card-list" aria-hidden="true"></span> List Cars
            </NavLink>
        </div>

    </nav>
</div>

<style>
    /* Simple styles that won't break mobile functionality */
    .nav-scrollable {
        max-height: 100%;
        overflow-y: auto;
    }

    .nav-category {
        padding: 8px 16px;
        margin-top: 8px;
        font-weight: 600;
        color: #ffffff;
        background-color: #4a6da7;
        font-size: 0.9rem;
        text-transform: uppercase;
    }

    .nav-link {
        display: flex;
        align-items: center;
        padding: 8px 12px;
        color: #495057;
    }

        .nav-link:hover {
            background-color: #f1f3f5;
            border-radius: 4px;
        }

        .nav-link.active {
            background-color: #0d6efd;
            color: white;
            border-radius: 4px;
        }

        .nav-link .bi {
            margin-right: 8px;
        }
</style>
